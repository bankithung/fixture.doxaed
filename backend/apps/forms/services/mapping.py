"""TEMPORARY mapping stub.

Increment 5 replaces this with purpose-dispatched entity mapping (reusing
``apps/teams`` ``register_school`` for ``team_registration``). For now it is a
no-op so the public submission flow (Increment 4) is green on its own.
"""
from __future__ import annotations


def map_response(resp):
    return resp  # replaced in Increment 5
