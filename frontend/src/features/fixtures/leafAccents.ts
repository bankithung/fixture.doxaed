/** Token-only accent palette — one left-border colour per competition so
 * multi-sport cards stay scannable (no hardcoded hex, design system rule). */
export const LEAF_ACCENTS = [
  "border-l-primary",
  "border-l-info",
  "border-l-success",
  "border-l-warning",
  "border-l-destructive",
] as const;
